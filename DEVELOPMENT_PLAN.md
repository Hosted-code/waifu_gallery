# Waifu Gallery 开发计划

## P0 — 基础浏览能力

- [x] **P0-1 应用内图片查看器** — 点击缩略图进入全屏/大图查看；鼠标滚轮缩放（适应窗口/实际大小/自由缩放）；拖拽平移；左右箭头键/滚轮切换图片；ESC 退出；GIF/WebP 动图播放；同一作品多图切换；沉浸模式（F11 全屏，鼠标移到边缘才出现控制栏）；预加载相邻图片保证切换流畅
- [x] **P0-2 元数据信息面板** — 查看器右侧或底部可折叠面板；文件信息（分辨率/大小/格式/路径）；来源信息（平台/作品 ID/作者）；标签列表（AI 标签 + 平台标签）；社交数据（点赞/收藏/转发）；日期（下载时间/编辑时间）
- [ ] **P0-3 手动标签管理** — 查看器中/缩略图右键菜单添加/删除标签；输入时自动补全已有标签（显示别名和父标签）；新标签自动创建写入数据库；删除标签从 picture_tags 移除关联；同步修改元数据文件

## P1 — 标签体系深度

- [ ] **P1-1 父子级标签** — 标签可有多父标签（多继承）；搜索父标签自动包含所有子标签结果（递归 CTE）；UI 中树形结构展示标签层级；例：亚丝娜 → 继承自 → Sword Art Online、角色
- [ ] **P1-2 同义标签（别名）** — 一个主标签可有多个别名；搜索任意别名自动匹配主标签；添加标签时自动归一化到主标签；UI 中显示"主标签名 (别名1, 别名2)"；例：结城明日奈 / 亚丝娜 / Asuna → 统一为 Asuna
- [ ] **P1-3 标签浏览器（一级页面）** — 标签管理从侧边栏附属功能升级为独立全屏视图；按分类（角色/属性/作品/画师）浏览标签；拖拽标签建立父子关系；显示每个标签的图片数量；支持标签搜索和筛选
- [ ] **P1-4 AI 标签审核工作流** — 利用现有 auto-tagger，增加人确认环节；AI 只出候选标签，按置信度排序显示；支持按置信度阈值批量 Accept/Reject；审核结果才写入数据库

## P2 — 搜索与管理增强

- [ ] **P2-1 Tag Query Language** — 搜索成为软件核心能力；支持 `tag:xxx`、`-tag:xxx`、`rating:>=4`、`favorite:true`、`color:blue` 等结构化查询；AND/OR/NOT 逻辑组合；括号嵌套分组
- [ ] **P2-2 收藏/评分 + Boss Key** — 缩略图上收藏按钮；1-5 星评分（数字键快捷操作）；收藏/评分筛选；Boss Key 一键隐藏/锁定 R18 内容，密码解锁隔离库
- [ ] **P2-3 EXIF / AI 生成元数据解析** — 读取图片 EXIF 信息；解析 Stable Diffusion / NovelAI / ComfyUI / A1111 的 Prompt、Negative Prompt、Steps、Sampler、CFG、Seed、Model、LoRA、VAE；点击 LoRA/Model 可查找所有使用该模型的图片
- [ ] **P2-4 智能文件夹** — 将搜索条件保存为"智能文件夹"；自动更新（新增符合条件的图片自动出现）；支持多条件组合
- [ ] **P2-5 键盘导航** — 箭头键在网格中移动选中；Enter 打开查看器；Space 预览；数字键 1-5 评分；ESC 返回
- [ ] **P2-6 缩略图大小调节** — 滑块或快捷键调整缩略图大小；保存偏好设置；当前固定 250px 改为可配置

## P3 — 高级功能与生态

- [ ] **P3-1 标签包导入/导出** — 导出标签体系（层级 + 别名）为 JSON/TOML；导入 Danbooru/Gelbooru/Safebooru 等社区标签词库；导入后自动建立层级和别名关系
- [ ] **P3-2 图片相似度/去重** — 利用已有 feature_hash 实现 Hamming 距离搜索；区分精确重复（xxHash）和相似图片（pHash）；重复图片合并/替换提示
- [ ] **P3-3 颜色搜索** — 导入时预计算图片主色调；颜色选取器搜索相似配色图片
- [ ] **P3-4 标签关系图** — 点一个标签不仅显示图片，还显示 Parent/Aliases/Related；可视化标签知识图谱
- [ ] **P3-5 UI 分叉** — 功能成型后做 QML vs Tauri 原型对比；选视觉更好的方案正式分叉

## 执行原则

- 一次只做一件事，做完验证再做下一个
- 新功能代码与 UI 解耦，确保未来换 UI 技术栈时只需重写表现层
- 不花精力美化当前 Qt UI，美观留给 UI 分叉阶段
